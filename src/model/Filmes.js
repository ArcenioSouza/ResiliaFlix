class Filmes {

   constructor(id, titulo, url, descricao, ano, tempo, elenco, genero){
      this._id = id,
      this._titulo = titulo,
      this._url = url,
      this._descricao = descricao,
      this._ano = ano,
      this._tempo = tempo,
      this._elenco = elenco,
      this._genero = genero
   }
}